# DOC-RECONCILIATION.md

This file was generated by the `/doc-truth-up` documentation-reconciliation pass, which treats
the code as read-only ground truth and edits only documentation so it reflects the repo's actual
current state. It changed no source code, configs, tests, or lockfiles, and ran no builds or
test suites. Its purpose is to give any reviewer an auditable record of what was checked, what
matched, and what still needs human attention.

---

## Per-Claim Findings

### 1. What It Is

**Status:** `consistent`
**Evidence basis:** `verified-by-reading-code`

The README describes the project as a "GitHub portfolio operating system" with a CLI tool
(`audit`) supporting four explicit product modes (First Run, Weekly Review, Deep Dive, Action
Sync). Entry point confirmed at `src/cli.py` (pyproject.toml `[project.scripts] audit =
"src.cli:main"`). The lead paragraph and "What This Project Is Today" section accurately
reflect the breadth of functionality present in the 100+ source modules under `src/`.

### 2. Current State

**Status:** `consistent`
**Evidence basis:** `verified-by-reading-code`

Verified claims:
- **12 analyzers** — `src/analyzers/` contains exactly 12 implementation modules (completeness,
  code_quality, interest, structure, description_analyzer, readme, security, activity, cicd,
  community_profile, dependencies, testing) plus an abstract `base.py`. CLI comment at
  `src/cli.py:8` confirms "Run all 12 analyzers". (`src/analyzers/*.py`)
- **15 achievement badges** — `src/badges.py` defines exactly 15 badge entries via the `@_def`
  decorator (fully-tested, well-documented, ci-champion, dependency-disciplined, fresh,
  battle-tested, complete-package, novel-tech, storyteller, community-ready, zero-debt,
  actively-maintained, built-to-ship, polyglot, has-fans). (`src/badges.py:20–97`)
- **SQLite history warehouse** — `src/history.py` present; README's "Storage: SQLite" claim
  is accurate.
- **`--portfolio-context-recovery` mode (Phase 104)** — flag and handler confirmed in
  `src/cli.py:393` and `src/cli.py:5235`.
- **`--portfolio-truth` path** — flag confirmed at `src/cli.py:390`; output path
  `output/portfolio-truth-latest.json` confirmed at `src/cli.py:2669`.
- **Baseline contract tracking** — `src/baseline_context.py` present and imported.
- **Watch mode with adaptive/incremental/full strategies** — `src/watch.py` present; strategy
  flag confirmed in CLI.
- **Workbook two modes (standard / template)** — `src/excel_template.py` present; flag
  `--excel-mode` confirmed in CLI.
- **Broad test suite** — 100+ test files present under `tests/` covering all major subsystems.
- **Four product mode guide** — `docs/modes.md` exists.

### 3. Stack

**Status:** `consistent`
**Evidence basis:** `verified-by-reading-code`

README tech-stack table checked against `pyproject.toml`:

| README claim | Evidence |
|---|---|
| Python 3.11+ | `pyproject.toml:7 requires-python = ">=3.11"` |
| GitHub API: REST v3 + GraphQL | `src/graphql_client.py` present; `requests` in dependencies |
| Excel output: openpyxl | `pyproject.toml:33 "openpyxl>=3.1.0"` |
| PDF output: fpdf2 | `pyproject.toml:38 "fpdf2>=2.7.0"` |
| AI narrative: Anthropic Claude API | `pyproject.toml:36 "anthropic>=0.40.0"` |
| Complexity analysis: Radon | `pyproject.toml:35 "radon>=6.0.0"` |
| CLI output: Rich | `pyproject.toml:34 "rich>=13.0"` |
| Storage: SQLite (history warehouse) | `src/history.py` present |

All correct.

### 4. How to Run

**Status:** `consistent`
**Evidence basis:** `verified-by-reading-code`

Checked each documented command:

- `audit run / triage / report / serve` subcommands — all defined in `src/cli.py`.
- Default serve address `http://127.0.0.1:8080/` — `src/serve/app.py:31` default `host="127.0.0.1"`, `port=8080`.
- `pytest` — `pyproject.toml` sets `testpaths = ["tests"]`.
- `make demo` — `Makefile:22` defines `demo` target.
- `make workbook-gate` — `Makefile:28` defines `workbook-gate` target.
- `make workbook-signoff` — `Makefile:31` defines `workbook-signoff` target.
- `make release-gate` — `Makefile:49` defines `release-gate` target.
- Install via `uv tool install github-repo-auditor` / `pipx` — PyPI name and entry point
  confirmed in `pyproject.toml`.
- `requirements.txt` described as "synchronized compatibility mirror" — file exists at repo root.

All correct.

### 5. Known Risks

**Status:** `consistent` (no README↔code contradictions found)
**Evidence basis:** `verified-by-reading-code`

No contradictions between the README and the current code. One severely outdated file
(`IMPLEMENTATION-ROADMAP.md`) contains multiple false claims about the codebase — see
"Contradictions for Manual Review" below. That file is outside the edit scope of this pass.

### 6. Next Move

**Status:** `unverifiable`
**Evidence basis:** `unverifiable-because-forward-looking`

The README does not contain an explicit roadmap or "next move" section. `IMPLEMENTATION-ROADMAP.md`
has a roadmap but it is so far out of date that its phased plan cannot be verified against any
current state without significant manual re-mapping. Flagged below for human review rather than
guessed at here.

---

## Contradictions for Manual Review

The following file is **outside the allowed edit scope** of this reconciliation pass. The drift
is documented here for a human to apply.

### `IMPLEMENTATION-ROADMAP.md`

This file was written for the project's early scaffolding phase and has never been updated. It
now contains multiple factual contradictions with the current codebase:

| Location | Claim in file | Reality (code evidence) |
|---|---|---|
| `IMPLEMENTATION-ROADMAP.md:131` | "Two dependencies. Everything else is stdlib." | `pyproject.toml` lists 9 core runtime dependencies plus `[serve]`, `[semantic]`, and `[dev]` extras. |
| `IMPLEMENTATION-ROADMAP.md:50` | "No database. All data flows through Python dataclasses in memory." | `src/history.py` implements a full SQLite history warehouse; `src/warehouse.py` (and related modules) present. |
| `IMPLEMENTATION-ROADMAP.md:44` | File tree shows `CLAUDE.md` as a root-level file. | No `CLAUDE.md` exists at the repo root. |
| `IMPLEMENTATION-ROADMAP.md:150` | "Out of scope: Web UI or dashboard (output is files only)" | `src/serve/` implements a FastAPI web UI with HTML dashboard; `audit serve` is a first-class subcommand. |
| `IMPLEMENTATION-ROADMAP.md:144` | "Score across 10 dimensions" | 12 analyzer dimensions are implemented (`src/analyzers/`). |
| `IMPLEMENTATION-ROADMAP.md:137` | Scope boundaries describe a thin fetch+clone+score pipeline with no campaigns, governance, Notion, or GitHub Projects | All of those features are fully implemented across dozens of source modules. |

**Recommended action:** Either archive `IMPLEMENTATION-ROADMAP.md` (move it to `docs/project-history.md`
or similar) or replace its content with a brief note pointing to `docs/project-history.md` and
`docs/modes.md` for the current operational guide.

---

## Footer

- **Date/time stamp:** 2026-05-30 04:14:45 PDT
- **Branch:** docs/truth-up-2026-05-30
- **HEAD sha reconciled against:** 04c692db13c4b6f51411b9ac7952a5512792e52f
